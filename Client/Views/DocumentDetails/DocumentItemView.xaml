<Window x:Class="Client.Views.DocumentDetails.DocumentItemView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Document Item" Height="200" Width="400"
        WindowStartupLocation="CenterScreen" Background="#FFD56AFF">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Product:" FontWeight="Bold"/>
        <TextBlock Text="{Binding ProductName}" Grid.Row="0" Margin="100,0,0,0"/>

        <TextBlock Text="Unit:" FontWeight="Bold" Grid.Row="1"/>
        <TextBlock Text="{Binding Unit}" Grid.Row="1" Margin="100,0,0,0"/>

        <TextBlock Text="Quantity:" FontWeight="Bold" Grid.Row="2"/>
        <TextBlock Text="{Binding Quantity}" Grid.Row="2" Margin="100,0,0,0"/>
    </Grid>
</Window>
